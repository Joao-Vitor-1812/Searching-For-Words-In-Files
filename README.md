# Searching-For-Words-In-Files
This project implements an information retrieval engine designed to perform fast, boolean-based searches across collections of unstructured legal documents (such as court petitions, legal opinions, and briefs). The system uses an Inverted Index backed by a Ternary Search Tree (TST) as its core data structure.
